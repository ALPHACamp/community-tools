docker build . -t community-tool \
  --build-arg APP_ID=example \
  --build-arg DISCORD_TOKEN=example \
  --build-arg PUBLIC_KEY=example \
  --build-arg DISCORD_GUILDID=example \
  --build-arg CHANNEL_ID=example \
  --build-arg PORT=example \